# Copia a web/.env y pega el mismo Project ID que en studio/.env (sanity.io/manage → API)
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
# Opcional: solo lectura pública en el navegador si usas token
# PUBLIC_SANITY_API_VERSION=2024-01-01
